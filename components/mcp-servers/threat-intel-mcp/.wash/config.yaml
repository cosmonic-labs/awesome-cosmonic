# wash v2 project configuration — used by Cosmonic Desktop's project mode
# (build + dev loop + promote) and by the `wash` CLI alike.
version: "0.1.0"
build:
  command: cargo build --target wasm32-wasip2 --release
  component_path: target/wasm32-wasip2/release/threat_intel_mcp.wasm
# Mirrors WorkloadDeployment components[].localResources; carried over into
# the durable Workload draft when you promote this project.
workload:
  environment:
    config:
      RUST_LOG: info
  # Outbound HTTP allowlist. Explicit [] is deny-all (fail-closed). This is the
  # egress boundary: the tools reach ONLY api.osv.dev and nothing else.
  allowedHosts:
    - api.osv.dev
