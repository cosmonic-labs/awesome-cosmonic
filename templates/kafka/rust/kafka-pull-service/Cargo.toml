[package]
name = "kafka-pull-service"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.58.0", features = ["macros", "async-spawn", "inter-task-wakeup"] }
